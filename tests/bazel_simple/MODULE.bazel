module(
    name = "bazel_simple_test",
    version = "0.1.0",
)

# Direct dependencies
bazel_dep(name = "rules_cc", version = "0.2.14")
bazel_dep(name = "platforms", version = "0.0.10")

# EROS Forge provides additional build rules and toolchains
bazel_dep(name = "eros_forge", version = "0.1.0")

local_path_override(
    module_name = "eros_forge",
    path = "../..",
)
